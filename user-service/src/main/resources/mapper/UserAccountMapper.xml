<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lianyutian.xhs.user.repository.mybatis.UserAccountMapper">
    <resultMap id="UserAccountResultMap" type="com.lianyutian.xhs.user.model.entity.UserAccountEntity">
        <id column="id" property="id"/>
        <result column="email" property="email"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.lianyutian.xhs.user.model.entity.UserAccountEntity">
        insert into user_account(email, password_hash, status, created_at, updated_at)
        values(#{email}, #{passwordHash}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findByEmail" resultMap="UserAccountResultMap">
        select id, email, password_hash, status, created_at, updated_at
        from user_account
        where email = #{email}
        limit 1
    </select>

    <select id="findById" resultMap="UserAccountResultMap">
        select id, email, password_hash, status, created_at, updated_at
        from user_account
        where id = #{id}
        limit 1
    </select>

    <select id="lockById" resultType="java.lang.Long">
        select id
        from user_account
        where id = #{id}
        limit 1
        for update
    </select>
</mapper>
