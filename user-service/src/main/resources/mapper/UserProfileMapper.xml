<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lianyutian.xhs.user.repository.mybatis.UserProfileMapper">
    <resultMap id="UserProfileResultMap" type="com.lianyutian.xhs.user.model.entity.UserProfileEntity">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="nickname" property="nickname"/>
        <result column="avatar_url" property="avatarUrl"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <insert id="insert" parameterType="com.lianyutian.xhs.user.model.entity.UserProfileEntity">
        insert into user_profile(user_id, nickname, avatar_url, created_at, updated_at)
        values(#{userId}, #{nickname}, #{avatarUrl}, #{createdAt}, #{updatedAt})
    </insert>

    <select id="findByUserId" resultMap="UserProfileResultMap">
        select id, user_id, nickname, avatar_url, created_at, updated_at
        from user_profile
        where user_id = #{userId}
        limit 1
    </select>
</mapper>
