## 1. 项目初始化

- [x] 1.1 向 Maven 项目中添加 Spring Boot web, validation, security, MyBatis, MySQL, Redis, mail, OpenAPI, test, captcha, 和 JWT 依赖
- [x] 1.2 为认证、验证、安全、持久化、通用响应处理和集成适配器创建基础包结构
- [x] 1.3 为数据库、Redis、JWT 密钥、验证码和邮件提供商设置添加环境驱动的配置框架

## 2. 认证领域与持久化

- [x] 2.1 为 `user_account` (用户账户), `user_profile` (用户资料), `user_session` (用户会话), `user_refresh_token` (用户刷新令牌), `verification_code` (验证码), 和 `security_event` (安全事件) 定义数据库模式和 MyBatis 映射
- [x] 2.2 为账户状态、会话状态、刷新令牌状态、验证码目的和验证码状态实现领域枚举和持久化规则
- [x] 2.3 为验证码存储、验证码证明、速率限制和短期风险计数器实现 Redis 键约定和仓库助手

## 3. 验证与注册流程

- [x] 3.1 实现图像验证码的生成、存储、验证和一次性消耗流程
- [x] 3.2 实现邮箱验证码的颁发，包括替换语义、发送结果持久化、过期和尝试次数统计
- [x] 3.3 实现注册 API，以验证和消耗 `REGISTER` (注册) 验证码，并原子性地创建账户和资料

## 4. 基于会话的认证流程

- [x] 4.1 实现邮箱/密码登录，包括账户状态检查、会话创建、刷新令牌颁发和访问令牌生成
- [x] 4.2 实现刷新令牌轮换，包括条件状态转换、重放检测和会话状态检查
- [x] 4.3 实现注销和当前用户 API，使用已认证的会话上下文，而不是客户端提供的所有权数据

## 5. 风险控制与错误语义

- [x] 5.1 在验证码发送、登录、刷新、上传和其他敏感入口点应用速率限制和渐进式登录升级
- [x] 5.2 为登录失败、速率限制命中、验证码发送失败、刷新重放和会话吊销实现安全事件记录
- [x] 5.3 实现分层认证错误处理，在返回稳定的客户端可见业务结果的同时保留确切的内部原因

## 6. 验证与就绪

- [x] 6.1 为注册、登录、刷新轮换、注销、验证码流程、验证码生命周期和速率限制升级添加单元测试和集成测试
- [x] 6.2 为认证端点、令牌行为、验证码行为和公共错误代码添加 API 文档
- [x] 6.3 运行测试套件，并根据 `user-auth` (用户认证), `verification-code` (验证码), 和 `api-risk-control` (API 风险控制) 规范验证变更
